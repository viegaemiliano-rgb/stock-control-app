{
  "name": "stock-expiration-app",
  "version": "0.1.0",
  "private": true,
  // 1. URL pública: CAMBIA ESTO
  "homepage": "https://github.com/viegaemiliano-rgb/stock-control-app", 
  
  // ... otras dependencias ...

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    
    // 2. SCRIPTS DE DESPLIEGUE (AÑADE ESTO)
    "predeploy": "npm run build", 
    "deploy": "gh-pages -d dist"  
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    // ... otros devDependencies ...
  }
}
